spring:
  profiles:
    group:
      "local" : "local, jwt, oauth"
    active : local

---
spring:
  config:
    activate:
      on-profile: "local"

  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: hanbit
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 30000

  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5Dialect
    properties:
      hibernate:
        ddl-auto: update
        format_sql: true
        show_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: likelionync.moments@gmail.com
    password: rjpqoqbnaldtsqlk
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
